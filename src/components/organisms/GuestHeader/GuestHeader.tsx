import * as React from 'react';

import { Container } from './style';

export interface IGuestHeaderProps {
}

const GuestHeader: React.FC<IGuestHeaderProps> = props => {
  return (
    <Container>

    </Container>
  );
};

export default GuestHeader;
